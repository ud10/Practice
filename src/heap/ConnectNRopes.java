/**
 * 
 */
package heap;

import java.util.PriorityQueue;

/**
 * @author uditgupta
 * May 23, 2016 1:56:59 AM
 */
public class ConnectNRopes {
	/**
	 * There are given n ropes of different lengths, we need to connect these ropes into one rope. 
	 * The cost to connect two ropes is equal to sum of their lengths. We need to connect the ropes with 
	 * minimum cost.
	 */
	public static void main(String[] args) {
		

	}
	@SuppressWarnings("unused")
	private static int minCost(int[] a){
		int len = a.length;
		PriorityQueue<Integer> pq = new PriorityQueue<Integer>(len);
		int f = 1;
		return 0;
	}
}
